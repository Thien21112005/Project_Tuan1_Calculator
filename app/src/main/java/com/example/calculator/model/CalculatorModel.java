package com.example.calculator.model;

import java.util.Stack;

public class CalculatorModel {
    // --- HÀM EVALUATE: Xử lý chuỗi phép tính (Presenter đang gọi hàm này) ---
    public double evaluate(String expression) {
        return 0;
    }
}